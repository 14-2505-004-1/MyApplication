<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="npaka.net.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="eラーニングアプリ"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".info" />
        <activity android:name=".testmode" />
        <activity android:name=".result" />
        <activity android:name=".beforeTest" />
        <activity android:name=".help" />
        <activity android:name=".help_use" />
        <activity android:name=".help_kanpo" />
        <activity android:name=".help_zetsu" />
        <activity android:name=".Mypage"></activity>
    </application>

</manifest>